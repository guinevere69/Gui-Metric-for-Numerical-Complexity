# gui-rate-of-digit-change-function
Mathematical function defining the mean absolute rate of digit change between consecutive digits of an integer. Includes Python implementation and theoretical notes for D(n) and its variance-based extensions.
